First, make sure you've run the cmake build in Apps/Android/CMakeBuild.
Next, from this directory execute the following commands:

# configure
./configure_ant.sh
./configure_cmake.sh

# build
./compile.sh

# run on a device
./run.sh
