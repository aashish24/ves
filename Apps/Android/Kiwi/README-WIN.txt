First, make sure you've run the configure command in Apps/Android/CMakeBuild.
> configure.bat

Next, from this directory execute the following commands:

> configure_ant.bat
> configure_cmake.bat

# build
> compile.bat

# run on a device
> run.bat
